{
  "name": "scripting-ts",
  "version": "1.0.0",
  "description": "Example about how to use the Typescript SDK to create a script to automate interactions with DEXES",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && cp -r src/resources dist/",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@jibidieuw/dexes": "^0.0.1"
  },
  "devDependencies": {
    "@types/node": "^25.0.9",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
